# React + Vite

This template provides a minimal setup to get React working in Vite with HMR and some ESLint rules.

Currently, two official plugins are available:

- [@vitejs/plugin-react](https://github.com/vitejs/vite-plugin-react/blob/main/packages/plugin-react) uses [Babel](https://babeljs.io/) for Fast Refresh
- [@vitejs/plugin-react-swc](https://github.com/vitejs/vite-plugin-react/blob/main/packages/plugin-react-swc) uses [SWC](https://swc.rs/) for Fast Refresh

## Expanding the ESLint configuration

If you are developing a production application, we recommend using TypeScript with type-aware lint rules enabled. Check out the [TS template](https://github.com/vitejs/vite/tree/main/packages/create-vite/template-react-ts) for information on how to integrate TypeScript and [`typescript-eslint`](https://typescript-eslint.io) in your project.

## link de netlify 
https://nodogametfepico-delafuente.netlify.app/login

## Requisitos previos
Node.js (v18+ recomendado)

npm (v9+)

## Dependencias Principales

npm install axios
npm install bootstrap
npm install bootstrap-icons
npm install cors
npm install framer-motion
npm install lucide-react
npm install react@next react-dom@next
npm install react-bootstrap
npm install react-hook-form
npm install react-router-dom
npm install react-toastify
npm install sweetalert2
npm install yup

## Tecnologías Clave
React 19 (Beta)

Vite (Bundler)

Tailwind CSS (Estilos)

Bootstrap 5 (Componentes UI)

React Router v7 (Navegación)

Axios (HTTP Client)

Framer Motion (Animaciones)

React Hook Form + Yup (Formularios)

SweetAlert2 (Modales)

React Toastify (Notificaciones)

## Adicional

Proyecto configurado con ES Modules ("type": "module")

Usa ESLint para linting con configuraciones de React

Tailwind CSS integrado con PostCSS/Autoprefixer